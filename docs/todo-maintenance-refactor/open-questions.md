# Open questions

미결정 없음.

- 동일 모듈 안의 private helper로 분리해 독립 배포와 import 경계를 유지한다.
- 중앙/standalone의 의도된 중복은 유지한다.
- 기존 generated 지침 전달 assertion은 제품 계약 검증이므로 보존한다.
- schema와 출력은 유지하고 source digest 변경만 self-hosting render로 반영한다.
