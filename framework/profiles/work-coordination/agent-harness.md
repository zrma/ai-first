### work-coordination

`work-coordination`은 `verification`과 함께 선택하며 `.ai-first/work.py` 및
repository skill을 배포한다. jj clone을 이용해 독립 작업을 격리하고 수정 범위,
revision, 검증 증거와 통합 상태를 연결한다. 작업 상태와 packet은 repository 밖에서
관리한다. host가 agent 호출·중단을 소유하며 CLI는 filesystem sandbox나 publication
권한을 제공하지 않는다.
